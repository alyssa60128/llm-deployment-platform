global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: mock-inference
    metrics_path: /metrics

    static_configs:
      - targets:
          - mock-inference:8000